<div class="container">
        <h2 class="mt-5">{{title}}</h2>
        <div class="chart-container">
            <canvas id="categoryChart"></canvas>
        </div>
        <a href="/home" class="btn btn-primary mt-3">Back to Home</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/javascripts/chart-script.js"></script> <!-- Link to the external JS file -->
    <script>
        // Parse the category spending data passed from the server
        const categorySpending = JSON.parse(`{{{categorySpending}}}`);
        initializeChart(categorySpending); // Initialize the chart with the parsed data
    </script>